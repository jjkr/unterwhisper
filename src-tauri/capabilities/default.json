{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "transcription"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "opener:default",
    "notification:default"
  ]
}
